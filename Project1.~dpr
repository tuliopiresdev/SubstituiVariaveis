program Project1;

uses
  Forms,
  uPrincipal in 'Units\uPrincipal.pas' {Form1},
  uVisualizador in 'Units\uVisualizador.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
